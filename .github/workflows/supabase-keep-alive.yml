name: Supabase Keep Alive

on:
  schedule:
    # Runs automatically every 4 hours
    - cron: "0 */4 * * *"

  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase Database
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

        run: |
          set -e

          echo "================================"
          echo "Supabase Keep Alive Started"
          echo "Time: $(date -u)"
          echo "================================"

          test -n "$SUPABASE_URL" || {
            echo "ERROR: NEXT_PUBLIC_SUPABASE_URL is empty"
            exit 1
          }

          test -n "$SUPABASE_KEY" || {
            echo "ERROR: NEXT_PUBLIC_SUPABASE_ANON_KEY is empty"
            exit 1
          }

          RESPONSE=$(curl \
            --silent \
            --show-error \
            --fail-with-body \
            --request PATCH \
            "${SUPABASE_URL}/rest/v1/keep_alive?id=eq.1&select=id,last_ping" \
            -H "apikey: ${SUPABASE_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_KEY}" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=representation" \
            --data '{"last_ping":"now()"}')

          echo "Supabase Response:"
          echo "$RESPONSE"

          echo "================================"
          echo "Supabase Database Ping Successful"
          echo "================================"
